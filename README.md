# Test React Vite

Minimal Vite + React starter created by an automated assistant.

Run locally:

```powershell
# install deps
npm install

# dev server
npm run dev
```

Build:

```powershell
npm run build
npm run preview
```
